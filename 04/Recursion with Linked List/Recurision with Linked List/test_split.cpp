/*
Use this file to test split.cpp file.

Create linked lists and split them with your split() function.

Can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <cstddef>
// #include <iostream>
// using namespace std;

int main(int argc, char* argv[])
{
  // Node* test = 
  // Node* evens = NULL;
  // Node* odds = NULL;
  // split(test, odds,evens);
  //return 0;

  // Node* evens = NULL;
  // Node* odds = NULL;
  
  // Node n1(2,NULL);
  // Node n2(2,&n1);
  // Node n3(2,&n2);
  // Node n4(5,&n3);

  // Node* in = &n4;
  // split(in, odds, evens);

  // Node* next;

  // cout << "print evens: " << endl;
  // while (evens != nullptr)
  // {
  //   cout << evens->value << endl;
  //   next = evens -> next;
  //   evens = next;
  // }

  // cout << "print odds: " <<endl;
  // while (odds != nullptr)
  // {
  //   cout << odds->value << endl;
  //   next = odds -> next;
  //   odds = next;
  // }

}
